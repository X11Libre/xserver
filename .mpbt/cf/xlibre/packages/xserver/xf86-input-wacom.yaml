sources:
    git:
        url:    ${@SOLUTION::xlibre_git}/xf86-input-wacom.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/xlibre-${@basename}-*:refs/tags/xlibre-${@basename}-*

buildsystem: autotools

build-depends:
    - x11/xserver-sdk
    - 3rdparty/util-macros

ref: origin/master
