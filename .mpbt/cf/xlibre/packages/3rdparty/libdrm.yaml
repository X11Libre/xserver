provides: sys/libdrm
type: bundle
sources:
    git:
        url:    ${@SOLUTION::xlibre_git}/mirror.fdo.drm
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}
buildsystem: meson
ref: libdrm-2.4.121
